server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db:5432/alligator
    username: postgres
    password: 1234
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect: PostgreSQLDialect

jwt:
  secret: 'lolkek'